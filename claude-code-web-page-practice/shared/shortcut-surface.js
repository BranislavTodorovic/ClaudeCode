(function(){
  'use strict';
  var OS=window.OneSpace,esc=OS.escapeHtml;
  window.OneSpaceShortcutUI={card:function(link,anchor,favorite,recent){
    function button(action,label,icon){return '<button class="btn" type="button" data-space-action="'+action+'" data-id="'+esc(link.id)+'" aria-label="'+esc(label+' '+link.name)+'" title="'+esc(label)+'"'+(action==='favorite'?' aria-pressed="'+favorite+'"':'')+'>'+OS.iconSvg(icon)+'</button>';}
    if(link.favicon&&/^assets\/[\w/-]+\.(svg|png|webp|ico)$/.test(link.favicon))anchor=anchor.replace('<span class="mini-icon">','<span class="mini-icon"><img class="shortcut-favicon" src="'+esc(link.favicon)+'" alt="" loading="lazy">');
    return '<div class="space-shortcut'+(favorite?' is-favorite':'')+(recent?' is-recent':'')+'" draggable="true" data-drag-id="'+esc(link.id)+'">'+anchor+'<div class="domain-actions">'+button('favorite',favorite?'Unfavorite':'Favorite','star')+(link.custom?button('edit','Edit','edit'):'')+button('remove','Remove','trash')+button('up','Move earlier','arrowUp')+button('down','Move later','arrowDown')+'</div></div>';
  }};
  document.addEventListener('click',function(e){var b=e.target.closest('[data-space-action]');if(!b)return;var action=b.dataset.spaceAction,id=b.dataset.id;if(action==='edit')OS.editShortcut(id);if(action==='delete')OS.deleteShortcut(id);if(action==='favorite'){OS.toggleFavorite(id);OS.renderSpaceShortcuts();var next=Array.from(document.querySelectorAll('[data-space-action="favorite"]')).find(function(x){return x.dataset.id===id && x.offsetParent;});if(next)next.focus();}});
  OS.renderSpaceShortcuts();
  function removed(){var host=document.getElementById('removedShortcutsSettings'),state=OS.shortcutState();if(!host)return;var links=state.builtins.filter(function(l){return state.hiddenIds.includes(l.id);});host.innerHTML='<h2>Removed shortcuts</h2>'+(links.length?'<ul class="removed-shortcuts">'+links.map(function(l){return '<li><span>'+esc(l.name)+'</span><button class="btn" type="button" data-restore-shortcut="'+esc(l.id)+'">Restore '+esc(l.name)+'</button></li>';}).join('')+'</ul>':'<p>No removed built-in shortcuts.</p>');}
  function remove(id){var state=OS.shortcutState(),link=state.links.find(function(l){return l.id===id;});if(!link)return;if(link.custom){OS.deleteShortcut(id);return;}window.OneSpaceUI.open('Remove shortcut?','<p>Hide '+esc(link.name)+' from your shortcuts. You can restore it in Settings.</p>',function(){if(!OS.setHiddenShortcuts(Array.from(new Set(state.hiddenIds.concat(id)))))throw new Error('Shortcut could not be removed.');removed();OS.showToast('Shortcut removed. Restore it in Settings.');return true;},'Remove shortcut');}
  document.addEventListener('click',function(e){var b=e.target.closest('[data-space-action="remove"],[data-action="remove"],[data-restore-shortcut]');if(!b)return;if(b.dataset.restoreShortcut){var ids=OS.shortcutState().hiddenIds.filter(function(id){return id!==b.dataset.restoreShortcut;});if(OS.setHiddenShortcuts(ids)){removed();OS.showToast('Shortcut restored.');document.querySelector('#removedShortcutsSettings button')?.focus();}}else remove(b.dataset.id);});
  document.addEventListener('onespace:page-changed',removed);removed();
  function focusCard(id,action){var buttons=Array.from(document.querySelectorAll('[data-space-action],[data-action="menu"]'));var b=buttons.find(function(el){return el.dataset.id===id&&el.offsetParent&&(!action||el.dataset.spaceAction===action||el.dataset.action==='menu');});if(b)b.focus();}
  function move(id,direction,target){var state=OS.shortcutState(),link=state.links.find(function(l){return l.id===id;});if(!link)return;var peers=state.links.filter(function(l){return l.category===link.category&&window.OneSpaceShortcuts.space(l)===window.OneSpaceShortcuts.space(link)&&!state.hiddenIds.includes(l.id);}),index=peers.indexOf(link);target=target||peers[index+(direction==='up'?-1:1)]?.id;if(!target){OS.showToast('That shortcut is already at the edge.');return;}var other=peers.find(function(l){return l.id===target;});if(!other){OS.showToast('Reorder within the same shortcut category.');return;}var ids=window.OneSpaceShortcuts.reorder(state.links,id,target);if(OS.setShortcutOrder(ids)){OS.showToast('Shortcut moved.');focusCard(id,direction);} }
  window.OneSpaceShortcutUI.move=move;
  document.addEventListener('click',function(e){var b=e.target.closest('[data-space-action="up"],[data-space-action="down"]');if(b)move(b.dataset.id,b.dataset.spaceAction);});
  var dragged=null;
  document.addEventListener('dragstart',function(e){var card=e.target.closest('[data-drag-id]');if(!card)return;dragged=card.dataset.dragId;e.dataTransfer.effectAllowed='move';e.dataTransfer.setData('text/plain',dragged);card.classList.add('is-dragging');});
  document.addEventListener('dragover',function(e){if(dragged&&e.target.closest('[data-drag-id]')){e.preventDefault();e.dataTransfer.dropEffect='move';}});
  document.addEventListener('drop',function(e){var card=e.target.closest('[data-drag-id]');if(!dragged||!card)return;e.preventDefault();move(dragged,null,card.dataset.dragId);dragged=null;});
  document.addEventListener('dragend',function(){dragged=null;document.querySelectorAll('.is-dragging').forEach(function(el){el.classList.remove('is-dragging');});});
  document.addEventListener('error',function(e){if(e.target.matches?.('.shortcut-favicon'))e.target.remove();},true);
  // Directory menus retain focus and keyboard semantics after moving a card.
  document.addEventListener('keydown',function(e){var menu=e.target.closest('.card-menu');if(!menu)return;var items=Array.from(menu.querySelectorAll('button')),i=items.indexOf(e.target);if(['ArrowDown','ArrowUp','Home','End'].includes(e.key)){e.preventDefault();items[e.key==='Home'?0:e.key==='End'?items.length-1:(i+(e.key==='ArrowDown'?1:-1)+items.length)%items.length].focus();}if(e.key==='Escape'){menu.classList.remove('open');var toggle=menu.parentElement.querySelector('[data-action="menu"]');toggle?.setAttribute('aria-expanded','false');toggle?.focus();}});
  OS.renderSpaceShortcuts();
})();
