(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.OneSpaceShortcuts=api;})(typeof window!=='undefined'?window:this,function(){
  'use strict';
  function space(link){if(link.custom && ['work','personal','explore'].includes(link.space))return link.space;if(['Travel','Relax & Play','Gaming'].includes(link.category))return 'explore';return ['Shopping & Home','Daily','Smart Home'].includes(link.category)?'personal':'work';}
  function category(owner,value){if(owner==='explore')return ['Travel','Relax & Play','Gaming'].includes(value)?value:'Travel';if(owner==='personal')return ['Daily','Shopping & Home','Smart Home'].includes(value)?value:'Daily';return ['Travel','Relax & Play','Gaming','Daily','Shopping & Home','Smart Home'].includes(value)?'Development':value;}
  function sanitize(value,builtins,categories){var ids=new Set(),urls=new Set();builtins.forEach(function(l){ids.add(l.id);urls.add(new URL(l.url).href);});return (Array.isArray(value)?value:[]).reduce(function(out,item){if(!item || typeof item.name!=='string' || !item.name.trim() || typeof item.url!=='string' || !categories.includes(item.category))return out;var url;try{url=new URL(item.url);if(!/^https?:$/.test(url.protocol)||url.href.length>2048)return out;}catch(_){return out;}if(urls.has(url.href))return out;var id=typeof item.id==='string' && /^[\w-]{1,120}$/.test(item.id)?item.id:'custom-migrated-'+out.length;while(ids.has(id))id+='-copy';ids.add(id);urls.add(url.href);var owner=space(Object.assign({},item,{custom:true}));out.push({id:id,name:item.name.trim().slice(0,40),url:url.href,category:category(owner,item.category),space:owner,description:typeof item.description==='string'?item.description.slice(0,240):'',icon:'link',custom:true});return out;},[]);}
  function ordered(links,ids){var positions=new Map((ids||[]).map(function(id,i){return [id,i];}));return links.slice().sort(function(a,b){return (positions.get(a.id)??Infinity)-(positions.get(b.id)??Infinity);});}
  function reorder(links,id,target){var next=links.slice(),from=next.findIndex(function(l){return l.id===id;}),to=next.findIndex(function(l){return l.id===target;});if(from<0||to<0||space(next[from])!==space(next[to])||next[from].category!==next[to].category)return next.map(function(l){return l.id;});var item=next.splice(from,1)[0];next.splice(to,0,item);return next.map(function(l){return l.id;});}
  return {ordered:ordered,reorder:reorder,initialHidden:function(){return ["hm","zara","maxmara"];},space:space,category:category,sanitize:sanitize};
});
