/* Shared same-origin search. Domain adapters own all record reads and writes. */
(function(){
 'use strict';var OS=window.OneSpace,UI=window.OneSpaceUI,esc=OS.escapeHtml;
 async function request(path,signal,method){var response;try{response=await fetch('/api/'+path,{signal:signal,method:method||'GET',headers:{Accept:'application/json'}});}catch(e){if(e.name==='AbortError')throw e;throw Object.assign(new Error('Search is offline. Your local catalog remains available.'),{code:'offline'});}var data;try{data=await response.json();}catch(_){throw Object.assign(new Error('Search is unavailable. Start the OneSpace server to use provider search.'),{code:'offline'});}if(!response.ok)throw Object.assign(new Error(data.message||'Search failed.'),{code:data.code||'provider-error'});return data;}
 function notice(error){return String(error.code||'provider-error').replace(/-/g,' ').replace(/^./,function(c){return c.toUpperCase();})+': '+error.message;}
 function formFields(domain,x){return domain==='games'?UI.select('tracker','Tracker type',[['story','Chapters / Campaign'],['weekly','Weekly / Live service']],x.trackerType||'story')+'<p>You can change this choice before adding the game.</p>':UI.select('saveStatus','Add as',[['unwatched','In library'],['watchlist','On watchlist'],['watched','Watched']],'unwatched');}
 function open(domain,x,adapter,done){var extra=formFields(domain,x);window.OneSpaceDiscovery.openDetail(x,adapter.has(x)?null:function(record,f){if(adapter.save(record,f)===false)throw new Error('The record could not be saved. Please try again.');if(done)done();return true;},domain==='games'?'Add game':'Add title',extra);}
 function bind(options){var input=options.input,dropdown=options.dropdown;if(!input||!dropdown||input.dataset.providerBound)return;input.dataset.providerBound='true';input.placeholder=options.domain==='games'?'Search games…':'Search movies & series…';
  var timer,controller,ticket=0,local=[],remote=[],next=null,active=0,query='',state='',rows=[];
  var live=document.createElement('p');live.className='provider-search-status';live.setAttribute('role','status');live.setAttribute('aria-live','polite');input.parentElement.appendChild(live);
  function close(){clearTimeout(timer);if(controller)controller.abort();ticket++;dropdown.hidden=true;input.setAttribute('aria-expanded','false');input.removeAttribute('aria-activedescendant');}
  function highlight(index){var elements=dropdown.querySelectorAll('[role="option"]');if(!elements.length)return;active=(index+elements.length)%elements.length;elements.forEach(function(el,i){el.setAttribute('aria-selected',String(i===active));});input.setAttribute('aria-activedescendant',elements[active].id);}
  function row(x,provider){var index=rows.length;rows.push({item:x,provider:provider});return '<li role="option" class="provider-search-option" id="'+input.id+'-option-'+index+'" data-search-index="'+index+'" aria-selected="false"><span class="provider-result-icon">'+OS.iconSvg(options.domain==='games'?'gamepad':'film')+'</span><span><strong>'+esc(x.name)+'</strong><small>'+esc((x.kind==='series'?'Series':x.kind==='movie'?'Movie':'Game')+(x.year?' · '+x.year:'')+(provider?' · '+x.source.name:' · Local'))+'</small></span><span class="provider-add">'+(options.adapter().has(x)?'Details':'Review & add')+'</span></li>';}
  function action(label,type){var index=rows.length;rows.push({action:type});return '<li role="option" class="provider-search-option" id="'+input.id+'-option-'+index+'" data-search-index="'+index+'" aria-selected="false">'+esc(label)+'</li>';}
  function render(){rows=[];var html='<li role="presentation" class="provider-result-heading">In your catalog</li>'+local.map(function(x){return row(x,false);}).join('');if(!local.length)html+='<li role="presentation" class="provider-result-empty">No local matches.</li>';html+='<li role="presentation" class="provider-result-heading">Search results</li>'+remote.map(function(x){return row(x,true);}).join('');if(state)html+='<li role="presentation" class="provider-result-empty">'+esc(state)+'</li>';if(next)html+=action('Load more search results','more');html+=action('Add custom '+(options.domain==='games'?'game':'title')+': '+query,'custom');dropdown.innerHTML=html;dropdown.hidden=false;input.setAttribute('aria-expanded','true');highlight(Math.min(active,rows.length-1));live.textContent=state;}
  async function search(append){if(controller)controller.abort();controller=new AbortController();var current=++ticket;state=options.domain==='games'?'Searching new worlds…':'Raising the curtain…';render();dropdown.setAttribute('aria-busy','true');try{var data=await request('search/'+options.domain+'?'+new URLSearchParams({q:query,page:append?next:1}),controller.signal);if(current!==ticket)return;var combined=append?remote:[],ids=new Set(combined.map(function(x){return x.id;}));remote=combined.concat(data.items.filter(function(x){return !ids.has(x.id);}));next=data.nextPage;state=(data.mode==='mock'?'Test provider · ':'')+(remote.length?remote.length+' provider results'+(data.cached?' · Cached':'')+'.':'No provider matches.');render();}catch(e){if(e.name!=='AbortError'&&current===ticket){state=notice(e);render();}}finally{if(current===ticket)dropdown.removeAttribute('aria-busy');}}
  async function select(index){var row=rows[index];if(!row)return;if(row.action==='more'){search(true);return;}if(row.action==='custom'){var name=query;close();options.custom(name);return;}var x=row.item;close();if(row.provider){controller=new AbortController();var current=++ticket;live.textContent='Opening details…';try{x=(await request('details/'+x.kind+'/'+x.id.split('-').pop(),controller.signal)).item;if(current!==ticket)return;}catch(e){if(e.name!=='AbortError')live.textContent=notice(e);return;}}live.textContent='';open(options.domain,x,options.adapter(),function(){input.value='';live.textContent=x.name+' added.';});}
  input.addEventListener('input',function(){clearTimeout(timer);if(controller)controller.abort();ticket++;dropdown.removeAttribute('aria-busy');query=input.value.trim();remote=[];next=null;active=0;if(!query){close();live.textContent='';return;}local=options.local(query);state='Waiting to search…';render();timer=setTimeout(function(){search(false);},280);});
  input.addEventListener('keydown',function(e){if(dropdown.hidden)return;if(e.key==='ArrowDown'||e.key==='ArrowUp'){e.preventDefault();highlight(active+(e.key==='ArrowDown'?1:-1));}else if(e.key==='Enter'){e.preventDefault();select(active);}else if(e.key==='Escape'){e.preventDefault();close();}});
  dropdown.addEventListener('click',function(e){var row=e.target.closest('[data-search-index]');if(row){e.stopPropagation();select(Number(row.dataset.searchIndex));}});
  document.addEventListener('click',function(e){if(!input.parentElement.contains(e.target))close();});
  document.addEventListener('onespace:page-changed',close);
 }
 async function status(){var el=document.getElementById('providerStatus');if(!el)return;try{var x=await request('status');el.textContent=(x.mode==='mock'?'Test mode · ':'')+'Movies & Series: '+x.titles.replace(/-/g,' ')+'. Games: '+x.games.replace(/-/g,' ')+'. Local catalogs always remain available.';}catch(e){el.textContent=notice(e);}}
 window.OneSpaceProviderSearch={bind:bind,open:open,request:request,status:status};
 document.addEventListener('onespace:page-changed',function(e){if(e.detail.page==='settings')status();});status();
})();
