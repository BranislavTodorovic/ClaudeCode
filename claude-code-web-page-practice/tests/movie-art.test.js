const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm'),path=require('node:path');
const read=p=>fs.readFileSync(path.join(__dirname,'..',p),'utf8');
test('seed movie artwork uses the explicit licensing fallback without loading undersized legacy posters',()=>{const c={window:{}};vm.runInNewContext(read('movies/movies-data.js'),c);for(const m of c.window.SEED_MOVIES.filter(x=>x.type==='movie')){assert.equal(m.poster.kind,'generated');assert.equal(m.backdrop.kind,'generated');assert(!m.poster.src);}});
test('approved image pairs support responsive selection and intrinsic width limits',()=>{const s=read('movies/movies.js'),start=s.indexOf('  function imageSizeAttrs('),end=s.indexOf('\n  function ',start+10),c={esc:x=>x};vm.runInNewContext(s.slice(start,end),c);const attrs=c.imageSizeAttrs({src:'large.jpg',width:1000,height:1500,smallSrc:'small.jpg',smallWidth:300},'50vw');assert(attrs.includes('small.jpg 300w, large.jpg 1000w'));assert(attrs.includes('sizes="50vw"'));assert(attrs.includes('--mv-intrinsic-width:1000px'));assert(read('movies/movies.css').includes('max-width: min(100%,var(--mv-intrinsic-width,100%))'));});
test('fifteen complete series have useful filter metadata, provenance and scalable art',()=>{const c={window:{}};vm.runInNewContext(read('movies/movies-data.js'),c);const series=c.window.SEED_MOVIES.filter(m=>m.type==='series');assert.equal(series.length,15);for(const m of series){assert(m.seasons>0&&m.durationMinutes>0&&m.rating>0);assert(m.platforms.length&&m.genre.length&&m.moods.length&&m.tags.length);assert(m.blurb.length>150);assert.equal(m.poster.kind,'generated');assert.equal(m.backdrop.kind,'generated');assert(m.metadataUrl.startsWith('https://www.tvmaze.com/shows/'));}assert(!read('movies/movies-data.js').includes("kind:'placeholder'"));assert(series.some(m=>m.genre.includes('Animation')));assert(series.some(m=>m.genre.includes('Horror')));});
