(function(){
  'use strict';var OS=window.OneSpace,D=window.OneSpaceDiscovery;
  function attach(domain,page,adapter,prefix){var root=document.getElementById(page+'View');if(!root||root.querySelector('[data-global-discovery]'))return;var panel=root.querySelector('[data-panel="suggestions"]');if(!panel)return;
    var examples=document.createElement('details');examples.className='provider-local-examples';examples.innerHTML='<summary>More filters for the bundled catalog</summary>';while(panel.firstChild)examples.appendChild(panel.firstChild);panel.appendChild(examples);
    var host=document.createElement('section');host.dataset.globalDiscovery=domain;host.dataset.kind=domain;panel.prepend(host);D.mount(host,domain,adapter);
    var nav=root.querySelector('[data-'+prefix+'-tab="suggestions"]');if(nav)nav.textContent='Discover';
    var cta=document.createElement('button');cta.className='btn btn-primary';cta.type='button';cta.textContent=domain==='games'?'Discover games':'Discover movies & series';cta.onclick=function(){nav.click();host.scrollIntoView({block:'start'});};root.querySelector('.'+prefix+'-hero-actions')?.appendChild(cta);
    root.querySelectorAll('input[role="combobox"]').forEach(function(input){input.placeholder='Search your library and offline examples…';input.setAttribute('aria-label','Search local collection');});
    var overview=root.querySelector('[data-panel="overview"]');if(overview){var p=document.createElement('p');p.className='provider-credit';p.textContent='Spotlight and Discover use bundled entries. Add your own games and titles to grow the collection.';overview.prepend(p);}
  }
  function mount(){attach('games','games',window.OneSpaceGameDiscovery,'gv');attach('titles','movies',window.OneSpaceTitleDiscovery,'mv');}
  function titleOptions(root){root.querySelectorAll('option,.gv-pref-chip span,.mv-pref-chip span').forEach(function(el){var value=el.textContent;if(/^[a-z]/.test(value)){if(el.tagName==='OPTION'&&!el.hasAttribute('value'))el.setAttribute('value',value);el.textContent=D.label(value);}});}
  var scheduled=false;new MutationObserver(function(){if(scheduled)return;scheduled=true;requestAnimationFrame(function(){scheduled=false;titleOptions(document);});}).observe(document.body,{childList:true,subtree:true});
  document.addEventListener('onespace:page-changed',function(e){mount();var page=document.getElementById(e.detail?.page+'View');if(!page)return;page.classList.remove('scene-enter');if(!OS.prefersReducedMotion()){requestAnimationFrame(function(){page.classList.add('scene-enter');});setTimeout(function(){page.classList.remove('scene-enter');},1000);}});
  document.querySelectorAll('.page-hero').forEach(function(hero){hero.addEventListener('pointermove',function(e){if(e.pointerType!=='mouse'||OS.prefersReducedMotion())return;var r=hero.getBoundingClientRect();hero.style.setProperty('--scene-x',((e.clientX-r.left)/r.width-.5)*12+'px');hero.style.setProperty('--scene-y',((e.clientY-r.top)/r.height-.5)*8+'px');});hero.addEventListener('pointerleave',function(){hero.style.setProperty('--scene-x','0px');hero.style.setProperty('--scene-y','0px');});});
  var settings=document.querySelector('.settings-data');if(settings){var p=document.createElement('p');p.className='provider-credit';p.textContent='Discovery is fully local. No provider integrations, API keys, or automatic external requests. Clear the temporary search cache without changing your saved records.';var clear=document.createElement('button');clear.className='btn';clear.textContent='Clear discovery cache';clear.onclick=function(){D.clearCache();OS.showToast('Search cache cleared. Your saved records are preserved.');};settings.append(p,clear);}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();titleOptions(document);
})();
