/* Bundled discovery only. No fetch, credentials, sockets, or external catalog. */
(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.OneSpaceLocalDiscovery=api;})(typeof window!=='undefined'?window:this,function(){
  'use strict';
  function list(x){return Array.isArray(x)?x:x?[x]:[];}
  function media(x){return typeof x==='string'&&(/^assets\/[\w./-]+$/.test(x)&&!x.includes('..')||/^data:image\/(png|jpeg|webp);base64,[A-Za-z0-9+/=]+$/.test(x))?x:null;}
  function normalize(x,domain){
    if(x.discoveryRecord)return Object.assign({},x.discoveryRecord,{id:x.id});
    var kind=domain==='destinations'?'destination':domain==='games'?'game':String(x.type||'movie').toLowerCase(),name=x.name||x.title;
    var image=domain==='destinations'?x.image:domain==='games'?(x.cardArtwork||x.artwork):x.poster?.src;
    return {id:x.id,kind:kind,name:name,description:x.description||x.details||x.blurb||x.summary||x.tagline||'No description has been added to this local entry.',country:x.country||'',region:x.region||'',categories:list(x.categories||x.category),seasons:kind==='series'?x.seasons:null,travelSeasons:domain==='destinations'?list(x.seasons):[],duration:list(x.duration),budget:x.budget||'',styles:list(x.styles),climate:x.climate||'',interests:list(x.tags),genres:list(x.genres||x.genre),platforms:domain==='titles'?[]:list(x.platforms||x.platform),services:[],libraryLabels:domain==='titles'?list(x.platforms):[],developers:list(x.developer),publishers:list(x.publisher),tags:list(x.playstyles).concat(list(x.tags)),year:x.year||null,released:x.released||null,runtime:x.durationMinutes||null,rating:x.rating||null,language:x.language||'',episodes:x.episodes||null,seriesStatus:x.seriesStatus||null,lastAirDate:x.lastAirDate||null,cast:list(x.cast),creators:list(x.creators),coordinates:x.coordinates||null,website:x.website||x.links?.[0]?.url||'',image:media(image),photos:[],resources:x.resources||[],defaultTaskTemplates:x.defaultTaskTemplates,source:{name:x.id.startsWith('manual-')?'Your location':'Bundled local catalog',url:'',license:'Local artwork and editorial metadata. See the bundled asset source notes. Ratings and service labels are static, not current availability.'},notes:x.notes||'',matchExplanation:'Matches every selected local filter group.'};
  }
  function pool(domain,data){var groups=domain==='destinations'?[data.DESTINATIONS||[],(data.trips||[]).map(function(r){return r.destination;})]:domain==='games'?[data.games||[],data.DEFAULT_GAMES||[],data.SUGGESTION_CATALOG||[]]:[data.titles||[],data.SEED_MOVIES||[]];var ids=new Set();return groups.flat().filter(function(x){if(!x?.id||ids.has(x.id))return false;ids.add(x.id);return true;}).map(function(x){return x.kind?Object.assign({},x,{image:media(x.image)}):normalize(x,domain);});}
  function text(x){return String(x==null?'':x).toLocaleLowerCase().replace(/sci-fi/g,'science fiction').replace(/role-playing/g,'rpg').replace(/culture\/history/g,'culture');}
  function contains(values,wanted){return !wanted||list(values).some(function(v){return text(v).includes(text(wanted));});}
  function matches(x,p){return (!p.q||text([x.name,x.description,x.country,x.region,...list(x.genres),...list(x.tags)].join(' ')).includes(text(p.q)))&&(!p.type||p.type==='all'||x.kind===p.type)&&contains(x.categories,p.category)&&contains(x.region,p.region)&&contains(x.country,p.country)&&contains(x.travelSeasons,p.season)&&contains(x.duration,p.duration)&&contains(x.budget,p.budget)&&contains(x.styles,p.style)&&contains(x.climate,p.climate)&&contains(x.interests,p.interests)&&contains(x.genres,p.genre)&&contains(x.platforms,p.platform)&&contains(x.developers,p.developer)&&contains(x.publishers,p.publisher)&&contains(x.tags,p.tags)&&contains(x.language,p.language)&&(!p.year||x.year===Number(p.year))&&(!p.decade||(x.year>=Number(p.decade)&&x.year<Number(p.decade)+10))&&(!p.runtime||(x.runtime&&x.runtime<=Number(p.runtime)))&&(!p.rating||(x.rating&&x.rating>=Number(p.rating)))&&contains(x.libraryLabels,p.service);}
  function search(domain,p,data){var page=Number(p.page||1);if(!Number.isInteger(page)||page<1)throw new Error('Invalid page');var all=pool(domain,data).filter(function(x){return matches(x,p);}),start=(page-1)*6;return {items:all.slice(start,start+6),nextPage:start+6<all.length?page+1:null,total:all.length,mode:'local',source:'Bundled local catalog',explanation:'Bundled and user-added entries only. Every selected filter must match; missing metadata is excluded when its filter is active.'};}
  function create(getData,options){options=options||{};var cache=new Map(),now=options.now||Date.now,ttl=options.ttl||300000;
    return {clear:function(){cache.clear();},request:async function(domain,action,p,signal){if(signal?.aborted)throw Object.assign(new Error('Cancelled'),{name:'AbortError'});await Promise.resolve();if(signal?.aborted)throw Object.assign(new Error('Cancelled'),{name:'AbortError'});var data=getData(),key=JSON.stringify([domain,action,p,data]),hit=cache.get(key);if(hit&&hit.expires>now())return JSON.parse(hit.json.replace('"cached":false','"cached":true'));var result;if(action==='search')result=search(domain,p,data);else {var item=pool(domain,data).find(function(x){return x.id===p.id;});if(!item)throw Object.assign(new Error('This entry is no longer in the local collection.'),{code:'not-found'});result=action==='photos'?{photos:item.photos||[]}:{item:item};}result.cached=false;if(cache.size>100)cache.clear();cache.set(key,{expires:now()+ttl,json:JSON.stringify(result)});return result;}};
  }
  return {media:media,normalize:normalize,pool:pool,matches:matches,search:search,create:create};
});
