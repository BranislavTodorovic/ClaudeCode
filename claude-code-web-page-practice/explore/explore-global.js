(function(){
  'use strict';var OS=window.OneSpace,UI=window.OneSpaceUI,D=window.OneSpaceDiscovery,T=window.OneSpaceTrips,esc=OS.escapeHtml,key='orbit-trip-board',preferenceKey='orbit-explore-preferences',host=document.getElementById('exploreDestinations');
  var searchHost=document.createElement('div'),board=document.createElement('section');board.className='trip-board';board.setAttribute('aria-label','Trip board');host.append(searchHost,board);
  function loadPreferences(){var p=OS.safeGetJSON(preferenceKey,{});return {category:(p.categories||[])[0]||'',season:(p.seasons||[])[0]||'',duration:(p.duration||[])[0]||'',budget:(p.budget||[])[0]||'',style:(p.pace||[])[0]||'',climate:(p.climate||[])[0]||'',interests:(p.interests||[])[0]||'',departure:(p.departure||[])[0]||''};}
  function savePreferences(values){var map={category:'categories',season:'seasons',duration:'duration',budget:'budget',style:'pace',climate:'climate',interests:'interests',departure:'departure'},next={};Object.keys(map).forEach(function(input){var value=String(values[input]||'');if(value)next[map[input]]=[value];});return OS.safeSet(preferenceKey,JSON.stringify(next));}
  function clearPreferences(){return OS.safeSet(preferenceKey,'{}');}
  function rows(){return OS.safeGetJSON(key,[]).slice().sort(function(a,b){return a.order-b.order;});}
  function commit(next){if(!OS.safeSet(key,JSON.stringify(next))){OS.showToast('Changes could not be saved. Please try again.');return false;}render();discovery.refresh();return true;}
  function save(dest){var current=rows(),next=T.save(current,dest,OS.uid('trip'));if(commit(next)){OS.showToast(current.length===next.length?'Already on your board.':'Saved to your trip board.');return true;}return false;}
  function manual(existing){var draftImage=existing?.image||'',reading=false;UI.open(existing?'Edit location':'Add Location',UI.field('name','Location name',existing?.name,'text','required maxlength="160"')+UI.field('country','Country or region',existing?.country,'text','required maxlength="160"')+UI.field('description','Description',existing?.description,'textarea','required maxlength="20000"')+UI.select('category','Category',['place','city break','nature','beach','culture','food','wellness','adventure','museum','island','park'],existing?.category||'place')+UI.field('lat','Latitude',existing?.coordinates?.lat,'number','step="any" min="-90" max="90"')+UI.field('lon','Longitude',existing?.coordinates?.lon,'number','step="any" min="-180" max="180"')+UI.field('website','Website',existing?.website,'url','maxlength="2048"')+'<label>Upload a local image (PNG, JPEG or WebP; up to 1 MB)<input name="localImage" type="file" accept="image/png,image/jpeg,image/webp"></label><label><input name="removeImage" type="checkbox"> Remove current image</label>'+UI.field('notes','Location notes',existing?.notes,'textarea','maxlength="5000"'),function(f){if(reading)throw new Error('The image is still loading. Please save again in a moment.');var lat=f.get('lat'),lon=f.get('lon');if(Boolean(lat)!==Boolean(lon))throw new Error('Supply both latitude and longitude, or leave both blank.');var dest={id:existing?.id||OS.uid('manual'),kind:'destination',name:String(f.get('name')).trim(),country:String(f.get('country')).trim(),description:String(f.get('description')),category:String(f.get('category')),coordinates:lat?{lat:Number(lat),lon:Number(lon)}:null,website:String(f.get('website')),image:f.get('removeImage')?'':draftImage,notes:String(f.get('notes')),source:{name:'Your location',url:String(f.get('website')),license:'User-supplied description and image.'}};if(!T.destination(dest))throw new Error('Check the location fields.');return existing?commit(rows().map(function(r){return r.destination.id===dest.id?Object.assign({},r,{destination:dest}):r;})):save(dest);},existing?'Save location':'Add to trip board');document.querySelector('#domainOverlay [name="localImage"]').onchange=function(e){var file=e.target.files[0];if(!file)return;if(!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>1048576){e.target.value='';document.getElementById('domainError').textContent='Choose a PNG, JPEG or WebP up to 1 MB.';return;}reading=true;var reader=new FileReader();reader.onload=function(){draftImage=reader.result;reading=false;};reader.onerror=function(){reading=false;document.getElementById('domainError').textContent='Image could not be read.';};reader.readAsDataURL(file);};}
  function edit(item,plan){var days=item.itinerary.map(function(d){return d.day+' | '+d.notes.replace(/\n/g,' ');}).join('\n');UI.open(plan?'Plan '+item.destination.name:'Edit trip',UI.select('status','Trip status',T.statuses,item.status)+UI.field('notes','Trip notes',item.notes,'textarea','maxlength="5000"')+UI.field('startDate','Start date',item.startDate,'date')+UI.field('endDate','End date',item.endDate,'date')+UI.select('priority','Priority',[['1','1 · Highest'],['2','2 · High'],['3','3 · Normal'],['4','4 · Low'],['5','5 · Someday']],String(item.priority))+UI.field('planTitle','Trip plan name',item.planTitle||(plan?item.destination.name+' trip':''),'text','maxlength="160"')+UI.field('itinerary','Day itinerary — one entry per line: day | notes',days,'textarea','maxlength="50000" placeholder="1 | Arrive and explore the neighborhood&#10;2 | Museum visit"'),function(f){var itinerary=String(f.get('itinerary')).split('\n').filter(function(line){return line.trim();}).map(function(line){var index=line.indexOf('|');if(index<0)throw new Error('Use day | notes for each itinerary entry.');return {day:Number(line.slice(0,index).trim()),notes:line.slice(index+1).trim()};});return commit(T.update(rows(),item.id,{status:String(f.get('status')),notes:String(f.get('notes')),startDate:String(f.get('startDate')),endDate:String(f.get('endDate')),priority:Number(f.get('priority')),planTitle:String(f.get('planTitle')),itinerary:itinerary}));},'Save trip');}
  function render(){var all=rows();board.innerHTML='<div class="discovery-heading"><div><span class="discovery-kicker">FROM MAYBE TO MEMORIES</span><h2>Your trip board</h2><p>'+all.length+' saved places · every note, date and day stays here</p></div></div>'+(!all.length?'<div class="trip-empty"><h3>Where will you go next?</h3><p>Search a place or add your own location, then turn an idea into a plan.</p><button class="btn btn-primary" data-board-search>Search destinations</button></div>':'<div class="trip-columns">'+T.statuses.map(function(status){var group=all.filter(function(r){return r.status===status;});return '<section class="trip-column"><h3>'+D.label(status)+' <span>'+group.length+'</span></h3>'+group.map(function(r){return '<article class="trip-card" data-trip-id="'+esc(r.id)+'">'+D.photo(r.destination)+'<div class="trip-card-copy"><h4>'+esc(r.destination.name)+'</h4><p>Priority '+r.priority+(r.startDate?' · '+esc(r.startDate):'')+(r.endDate?' → '+esc(r.endDate):'')+'</p><p>'+esc(r.notes||'Add your first trip note.')+'</p>'+UI.select('status','Status for '+r.destination.name,T.statuses,r.status)+(r.planTitle?'<h5>'+esc(r.planTitle)+'</h5><ol>'+r.itinerary.map(function(day){return '<li>Day '+day.day+': '+esc(day.notes)+'</li>';}).join('')+'</ol>':'')+'<div class="domain-actions"><button class="btn" data-trip="details">Details</button><button class="btn" data-trip="edit">'+OS.iconSvg('edit')+'Edit trip</button><button class="btn" data-trip="plan">'+OS.iconSvg('calendaricon')+(r.planTitle?'Edit plan':'Create trip plan')+'</button><button class="btn" data-trip="up" aria-label="Move '+esc(r.destination.name)+' earlier" title="Move earlier" '+(all.indexOf(r)===0?'disabled':'')+'>'+OS.iconSvg('arrowUp')+'</button><button class="btn" data-trip="down" aria-label="Move '+esc(r.destination.name)+' later" title="Move later" '+(all.indexOf(r)===all.length-1?'disabled':'')+'>'+OS.iconSvg('arrowDown')+'</button><button class="btn" data-trip="delete" aria-label="Remove '+esc(r.destination.name)+'" title="Remove destination">'+OS.iconSvg('trash')+'Remove</button></div></div></article>';}).join('')+(!group.length?'<p class="trip-column-empty">Ready for your next place</p>':'')+'</section>';}).join('')+'</div>');D.wirePhotos(board);}
  board.onchange=function(e){var card=e.target.closest('[data-trip-id]');if(card&&e.target.name==='status'){var id=card.dataset.tripId;if(commit(T.update(rows(),id,{status:e.target.value}))){board.querySelector('[data-trip-id="'+id+'"] select').focus();OS.showToast('Trip status updated.');}}};
  board.onclick=function(e){var b=e.target.closest('button');if(!b)return;if(b.hasAttribute('data-board-search')){discovery.search();return;}var card=b.closest('[data-trip-id]'),item=card&&rows().find(function(r){return r.id===card.dataset.tripId;});if(!item)return;var action=b.dataset.trip;if(action==='edit'||action==='plan')edit(item,action==='plan');if(action==='delete')UI.confirm('Remove destination','Remove '+item.destination.name+' and its trip plan from your board?',function(){return commit(rows().filter(function(r){return r.id!==item.id;}));});if(action==='up'||action==='down'){if(commit(T.move(rows(),item.id,action==='up'?-1:1)))board.querySelector('[data-trip-id="'+item.id+'"] button').focus();}if(action==='details'){if(item.destination.id.startsWith('manual-')){D.openDetail(item.destination,function(){setTimeout(function(){manual(item.destination);},0);},'Edit location');}else D.openDetail(item.destination);}};
  var discovery=D.mount(searchHost,'destinations',{snapshot:function(){return {DESTINATIONS:window.DESTINATIONS,trips:rows()};},save:save,manual:manual,has:function(x){return rows().some(function(r){return r.destination.id===x.id;});},loadPreferences:loadPreferences,savePreferences:savePreferences,clearPreferences:clearPreferences});
  // One-time non-destructive migration; old saved records and their notes remain in backups.
  if(OS.safeGet(key)==null){var legacy=OS.safeGetJSON('orbit-explore-saved',[]),migrated=[];legacy.forEach(function(s){var old=(window.DESTINATIONS||[]).find(function(d){return d.id===s.destinationId;});if(!old)return;var d={id:old.id,kind:'destination',name:old.name,country:old.country,description:old.details||old.summary,notes:s.notes,image:'',source:{name:'Previously saved local destination',url:old.links?.[0]?.url||'',license:'Local editorial record'}};migrated=T.save(migrated,d,s.id);});if(migrated.length)OS.safeSet(key,JSON.stringify(migrated));}
  document.querySelector('[data-action="destination-random"]').textContent='Surprise me';document.querySelector('[data-action="destination-random"]').onclick=function(){discovery.random();};render();
})();
