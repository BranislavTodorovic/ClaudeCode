(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.OneSpaceTrips=api;})(typeof window!=='undefined'?window:this,function(){
  'use strict';
  var statuses=['idea','researching','planned','visited'];
  function validUrl(x){try{return !x || /^https?:$/.test(new URL(x).protocol);}catch(e){return false;}}
  function validDate(x){if(!x)return true;var d=new Date(x+'T00:00:00Z');return /^\d{4}-\d\d-\d\d$/.test(x)&&Number.isFinite(+d)&&d.toISOString().slice(0,10)===x;}
  function destination(x){return x&&typeof x.id==='string'&&/^[\w-]+$/.test(x.id)&&typeof x.name==='string'&&!!x.name.trim()&&x.name.length<=160&&typeof x.description==='string'&&x.description.length<=20000&&['country','region','category','notes'].every(function(k){return x[k]==null||(typeof x[k]==='string'&&x[k].length<=(k==='notes'?5000:160));})&&validUrl(x.website)&&(!x.image||(/^assets\/[\w./-]+$/.test(x.image)&&!x.image.includes('..'))||(/^data:image\/(png|jpeg|webp);base64,[A-Za-z0-9+/=]+$/.test(x.image)&&x.image.length<1500000))&&(!x.coordinates||(Number.isFinite(x.coordinates.lat)&&Math.abs(x.coordinates.lat)<=90&&Number.isFinite(x.coordinates.lon)&&Math.abs(x.coordinates.lon)<=180));}
  function valid(x){return x&&typeof x.id==='string'&&destination(x.destination)&&statuses.includes(x.status)&&typeof x.notes==='string'&&x.notes.length<=5000&&Number.isInteger(x.priority)&&x.priority>=1&&x.priority<=5&&validDate(x.startDate)&&validDate(x.endDate)&&(!x.startDate||!x.endDate||x.endDate>=x.startDate)&&Number.isInteger(x.order)&&x.order>=0&&Array.isArray(x.itinerary)&&x.itinerary.length<=100&&x.itinerary.every(function(d){return Number.isInteger(d.day)&&d.day>=1&&d.day<=365&&typeof d.notes==='string'&&d.notes.length<=3000;})&&typeof x.planTitle==='string'&&x.planTitle.length<=160&&Number.isFinite(Date.parse(x.createdAt));}
  function save(rows,dest,id){if(!destination(dest))throw new Error('Invalid destination. Check its name, website, photo and coordinates.');if(rows.some(function(r){return r.destination.id===dest.id;}))return rows;return rows.concat([{id:id,destination:dest,status:'idea',notes:dest.notes||'',priority:3,startDate:'',endDate:'',order:rows.length,planTitle:'',itinerary:[],createdAt:new Date().toISOString()}]);}
  function update(rows,id,patch){return rows.map(function(r){if(r.id!==id)return r;var next=Object.assign({},r,patch);if(!valid(next))throw new Error('Check trip dates, priority and itinerary. End date must follow start date.');return next;});}
  function move(rows,id,delta){var ordered=rows.slice().sort(function(a,b){return a.order-b.order;}),from=ordered.findIndex(function(r){return r.id===id;}),to=from+delta;if(from<0||to<0||to>=ordered.length)return rows;var item=ordered.splice(from,1)[0];ordered.splice(to,0,item);return ordered.map(function(r,i){return Object.assign({},r,{order:i});});}
  return {statuses:statuses,valid:valid,destination:destination,save:save,update:update,move:move};
});
