(function(root,factory){var api=factory();if(typeof module==='object' && module.exports)module.exports=api;else{root.OneSpaceExplore=api;api.mount(root);}})(typeof window!=='undefined'?window:this,function(){
  'use strict';
  function recommend(catalog,prefs,validate) {
    return catalog.filter(function(d){return !validate || validate(d);}).map(function(d){
      var reasons=[],score=0,fields={categories:d.categories,seasons:d.seasons,duration:d.duration,budget:[d.budget],pace:d.styles,climate:[d.climate],interests:d.tags};
      var matches=Object.keys(fields).every(function(k){var selected=prefs[k] || [];if(!selected.length)return true;var hit=selected.filter(function(v){return (fields[k] || []).includes(v);});if(hit.length){score+=hit.length;reasons.push(k+': '+hit.join(', '));}return !!hit.length;});
      if(!matches)return null;
      if((prefs.departure || []).includes(d.region)){score+=1;reasons.push('Same broad departure region');}
      return {destination:d,score:score,reasons:reasons.length?reasons:['A starting point from the local catalog; choose preferences to refine it.']};
    }).filter(Boolean).sort(function(a,b){return b.score-a.score || a.destination.id.localeCompare(b.destination.id);});
  }
  function mount(root){
    var OS=root.OneSpace,UI=root.OneSpaceUI,esc=OS.escapeHtml,host=document.getElementById('exploreDestinations');if(!host)return;
    var pk='orbit-explore-preferences',sk='orbit-explore-saved',prefs=OS.safeGetJSON(pk,{}),catalog=(root.DESTINATIONS || []).filter(root.OneSpaceStorage.validDestination);
    var groups={categories:['city break','nature','beach','culture/history','food','wellness','adventure'],seasons:['spring','summer','autumn','winter'],duration:['weekend','week','extended'],budget:['low','medium','high'],pace:['relaxed','balanced','active'],climate:['warm','temperate','cool'],interests:['architecture','walking','museums','water','hiking','cuisine','spa','wildlife'],departure:['any','Europe','North America','Asia-Pacific']};
    var labels={categories:'Destination type',seasons:'Season',duration:'Trip length',budget:'Relative budget',pace:'Travel pace',climate:'Climate',interests:'Interests',departure:'Departure region assumption'};
    function saved(){return OS.safeGetJSON(sk,[]);}
    function art(d){return '<div class="destination-art" role="img" aria-label="Illustrated travel landscape for '+esc(d.name)+'">'+root.OneSpaceVisual.cover(d.fallback,'project','#729eac')+'<img src="'+esc(d.image)+'" alt="Travel inspiration illustration for '+esc(d.name)+'" loading="lazy" width="800" height="450"></div>';}
    function card(r,shortlist){var d=r.destination,isSaved=saved().some(function(s){return s.destinationId===d.id;});return '<article class="domain-card destination-card">'+art(d)+'<div class="destination-copy"><p class="domain-eyebrow">'+esc(d.country)+' · '+esc(d.budget)+' relative budget</p><h3>'+esc(d.name)+'</h3><p>'+esc(d.summary)+'</p><p class="match-reasons"><strong>Why this fits</strong><br>'+r.reasons.map(esc).join(' · ')+'</p><div class="domain-actions"><button class="btn" data-destination="'+esc(d.id)+'">'+OS.iconSvg('maps')+'Details</button><button class="btn" data-save-destination="'+esc(d.id)+'" aria-pressed="'+isSaved+'">'+OS.iconSvg('star')+(isSaved?'Remove from shortlist':'Save destination')+'</button></div>'+(shortlist?'<button class="btn" data-trip-notes="'+esc(d.id)+'">'+OS.iconSvg('edit')+'Trip notes</button><p class="domain-prose">'+esc((saved().find(function(s){return s.destinationId===d.id;}) || {}).notes || '')+'</p>':'')+'</div></article>';}
    function render(){
      var results=recommend(catalog,prefs),shortlist=saved();
      host.innerHTML='<div class="page-section-head"><div><h2>Your next change of scenery</h2><p>Curated ideas, shaped by what you enjoy.</p></div></div><form id="destinationPreferences" class="domain-filters">'+Object.keys(groups).map(function(k){return UI.select(k,labels[k],[['','Any']].concat(groups[k]),(prefs[k] || [])[0] || '');}).join('')+'<button class="btn btn-primary" type="submit">Find destinations</button><button class="btn" type="button" data-clear-destinations>Clear preferences</button></form><p>Budget is a relative local cost guide, excluding flights. Region affects ranking only; no location is detected. Check current conditions before booking.</p><p role="status">'+results.length+' matching destinations</p><div class="domain-grid">'+(results.length?results.map(function(r){return card(r,false);}).join(''):'<p class="proof-empty">No exact matches. Clear a preference to broaden your trip ideas.</p>')+'</div><h2>Your shortlist / trip board</h2><div class="domain-grid">'+(shortlist.length?shortlist.map(function(s){var d=catalog.find(function(d){return d.id===s.destinationId;});return d?card({destination:d,reasons:['Saved for a future trip.']},true):'<p>Destination no longer in the catalog: '+esc(s.destinationId)+'</p>';}).join(''):'<p class="proof-empty">Save a destination to start planning.</p>')+'</div>';
      host.querySelectorAll('.destination-art img').forEach(function(img){img.onerror=function(){img.remove();};});
    }
    function details(id){var d=catalog.find(function(d){return d.id===id;});if(!d)return;UI.open(d.name,art(d)+'<p>'+esc(d.details)+'</p><p>'+esc(d.categories.join(' · '))+'</p><p>Seasons: '+esc(d.seasons.join(', '))+' · Trip length: '+esc(d.duration.join(', '))+'</p>'+d.links.map(function(l){return '<a class="btn" href="'+esc(l.url)+'" target="_blank" rel="noopener noreferrer">'+esc(l.label)+'</a>';}).join(''));var img=document.querySelector('#domainOverlay img');if(img)img.onerror=function(){img.remove();};}
    host.onsubmit=function(e){e.preventDefault();var next={};new FormData(e.target).forEach(function(v,k){next[k]=v?[v]:[];});if(OS.safeSet(pk,JSON.stringify(next))){prefs=next;render();host.querySelector('[type="submit"]').focus();OS.showToast('Destination preferences saved.');}};
    host.onclick=function(e){var b=e.target.closest('button');if(!b)return;if(b.hasAttribute('data-clear-destinations')){if(OS.safeSet(pk,'{}')){prefs={};render();host.querySelector('[data-clear-destinations]').focus();}return;}if(b.dataset.destination)details(b.dataset.destination);if(b.dataset.saveDestination){var id=b.dataset.saveDestination,next=saved(),exists=next.some(function(s){return s.destinationId===id;});next=exists?next.filter(function(s){return s.destinationId!==id;}):next.concat([{id:OS.uid('trip'),destinationId:id,createdAt:new Date().toISOString(),notes:''}]);if(OS.safeSet(sk,JSON.stringify(next))){render();host.querySelector('[data-save-destination="'+id+'"]').focus();OS.showToast(exists?'Destination removed.':'Destination saved.');}}if(b.dataset.tripNotes){var dest=b.dataset.tripNotes,item=saved().find(function(s){return s.destinationId===dest;});UI.open('Trip notes',UI.field('notes','Ideas for this trip',item.notes,'textarea','maxlength="2000"'),function(f){var next=saved().map(function(s){return s.id===item.id?Object.assign({},s,{notes:String(f.get('notes'))}):s;});if(!OS.safeSet(sk,JSON.stringify(next)))return false;render();OS.showToast('Trip notes saved.');});}};
    document.querySelector('[data-action="destination-random"]').onclick=function(){var results=recommend(catalog,prefs);if(!results.length){OS.showToast('No matches. Broaden your preferences.');return;}details(results[Math.floor(Math.random()*results.length)].destination.id);};
    render();
  }
  return {recommend:recommend,mount:mount};
});
