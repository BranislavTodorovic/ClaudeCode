/* Settings owns presentation preferences; domain records are never reset here. */
(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else{root.OneSpaceSettings=api;api.mount(root);}})(typeof window!=='undefined'?window:this,function(){
 'use strict';
 var pages=['home','work','personal','explore','games','movies','shortcuts','productivity','notes','settings'];
 function defaults(){return {'orbit-theme':null,'orbit-palette':'auto','orbit-background':'orbit','orbit-accent':'blue','orbit-density':'comfortable','orbit-clock-format':'12','orbit-startup-mode':'full','orbit-minimal-mode':'false','orbit-widgets':'visible','orbit-motion-override':'auto','orbit-scene-intensity':null,'orbit-games-theme':'midnight','orbit-movies-theme':'marquee'};}
 function reset(storage,boundary){boundary.transaction(storage,defaults());}
 function mount(root){
  var OS=root.OneSpace,UI=root.OneSpaceUI,key='orbit-scene-intensity',grid=document.querySelector('#sceneIntensitySettings .scene-settings-grid');
  function title(page){return page==='movies'?'Movies & Series':page==='notes'?'Quick Notes':page.charAt(0).toUpperCase()+page.slice(1);}
  grid.innerHTML=pages.map(function(page){return '<label>'+title(page)+'<select data-scene-intensity="'+page+'" aria-label="'+title(page)+' scene intensity"><option value="full">Full</option><option value="subtle">Subtle</option><option value="off">Off</option></select></label>';}).join('');
  function sync(){var values=OS.safeGetJSON(key,{});grid.querySelectorAll('select').forEach(function(select){var page=select.dataset.sceneIntensity;select.value=values[page]||'full';var view=document.getElementById(page+'View');if(view)view.dataset.sceneIntensity=select.value;});document.body.dataset.sceneIntensity=values[document.body.dataset.page]||'full';document.getElementById('gamesThemeSetting').value=OS.safeGet('orbit-games-theme')||'midnight';document.getElementById('moviesThemeSetting').value=OS.safeGet('orbit-movies-theme')||'marquee';}
  grid.onchange=function(e){var page=e.target.dataset.sceneIntensity;if(!page)return;var values=OS.safeGetJSON(key,{});values[page]=e.target.value;if(OS.safeSet(key,JSON.stringify(values))){sync();document.dispatchEvent(new CustomEvent('onespace:motion-changed'));OS.showToast(title(page)+' scene updated.');}else sync();};
  ['games','movies'].forEach(function(domain){document.getElementById(domain+'ThemeSetting').onchange=function(){if(OS.safeSet('orbit-'+domain+'-theme',this.value)){document.dispatchEvent(new CustomEvent('onespace:subtheme-changed',{detail:{domain:domain,theme:this.value}}));OS.showToast(title(domain)+' theme updated.');}else sync();};});
  root.OneSpaceSettings.resetPreferences=function(){UI.open('Reset preferences?','<p>Restore appearance, scene, motion and display preferences. Saved records, favorites, recent links and search choices stay as they are.</p>',function(){try{reset(root.localStorage,root.OneSpaceStorage);OS.showToast('Preferences reset.');root.location.reload();return true;}catch(error){throw new Error('Preferences could not be reset: '+error.message);}},'Reset preferences');};
  // Roving radio focus supplies the keyboard behavior implied by the radiogroup role.
  document.querySelectorAll('.theme-choices,[aria-label="Background style"]').forEach(function(group){function tabs(){var buttons=Array.from(group.querySelectorAll('[role="radio"]'));buttons.forEach(function(b){b.tabIndex=b.getAttribute('aria-checked')==='true'?0:-1;});}group.addEventListener('keydown',function(e){if(!['ArrowLeft','ArrowRight','ArrowUp','ArrowDown','Home','End'].includes(e.key))return;var buttons=Array.from(group.querySelectorAll('[role="radio"]')),index=buttons.indexOf(e.target);if(index<0)return;e.preventDefault();var next=e.key==='Home'?0:e.key==='End'?buttons.length-1:(index+(e.key==='ArrowLeft'||e.key==='ArrowUp'?-1:1)+buttons.length)%buttons.length;buttons[next].click();buttons[next].focus();tabs();});group.addEventListener('click',tabs);tabs();});
  document.addEventListener('onespace:page-changed',sync);document.addEventListener('onespace:data-changed',function(e){if([key,'orbit-games-theme','orbit-movies-theme'].includes(e.detail.key))sync();});sync();
 }
 return {defaults:defaults,reset:reset,mount:mount};
});
